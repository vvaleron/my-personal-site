define([
	'angular',
	'app'
], 
function (ng, app) {
	'use strict';
	 var controller = app.controller('app-controller',
	    ['$scope', '$http', function($scope, $http) {
	        	debugger
	        }
	    ]
	);
	 return controller;
});